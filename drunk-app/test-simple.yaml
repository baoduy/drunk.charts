global:
  image: nginx
  tag: "1.25"

deployment:
  enabled: true
  ports:
    http: 80

ingress:
  enabled: true
  hosts:
    - host: "test.example.com"
      paths:
        - path: "/"
          pathType: "Prefix"
          port: 80
