# Minimal Configuration
# Only creates a GatewayClass, no Gateway resources

gatewayClass:
  enabled: true
  name: nginx
  controllerName: gateway.nginx.org/nginx-gateway-controller
  description: "NGINX Gateway Fabric GatewayClass"

gateway:
  enabled: false

certManager:
  enabled: false
